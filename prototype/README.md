# プロトタイプについて

画面イメージ・操作感の認識合わせ用の使い捨てプロトタイプです。本番実装ではありません。

- データベースへの保存は行わず、ブラウザの`localStorage`にデータを保持します（サーバー不要）
- ページを再読み込みしても内容は保持されますが、`localStorage`をクリアした場合や別のブラウザ・端末で開いた場合は初期データに戻ります
- `index.html` をブラウザで直接開けば動作します
