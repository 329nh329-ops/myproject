# Git / GitHub運用ルール

このプロジェクトでは、以下のルールを必ず守って開発を進める。

## 基本フロー

1. 作業を始める前に、GitHub Issueを作成する（バグ修正・機能追加・ドキュメント更新など、まとまった作業単位ごとに1つ）
2. Issueに対応する作業ブランチを作成する
3. 作業ブランチ上でコミットを積む
4. 作業が完了したらPull Requestを作成する
5. 内容を確認したうえで、自分（開発者本人）がPRをマージする

## ブランチ運用

- **mainブランチへの直接pushは禁止**。すべての変更はブランチを切ってPR経由でmainに取り込む
- ブランチ名は `feature/{Issue番号}-{短い説明（英語・ハイフン区切り）}` の形式にする
  - 例：Issue #12「カード編集機能を追加する」→ `feature/12-card-edit`
  - 機能追加以外（バグ修正等）でも `feature/` プレフィックスで統一してよい。プレフィックスの種類を増やすかどうかは今後の運用次第とする
- ブランチはIssueと1対1で対応させる。1つのブランチで複数のIssueを同時に扱わない

## Issue運用

- 作業に着手する前に、その作業内容をGitHub Issueとして登録する
- Issueのタイトルは、何を実現する作業なのかが一目でわかる短い文にする
- Issue番号は、対応するブランチ名・コミットメッセージ・PRの説明で参照し、作業の追跡ができるようにする

## Pull Request運用

- Pull Requestは対応するIssueへの参照を含める（例：本文に `Closes #12` と書き、マージ時にIssueが自動クローズされるようにする）
- レビュー担当者がいない個人開発のため、マージは開発者本人が内容を確認したうえで自分で行う
- 現時点ではCI（自動テスト等）は未整備のため、マージの必須条件には含めない。CIを整備した際は、その必須化についてあらためて検討する

## GitHub側の保護設定

- mainブランチには GitHub の Branch Protection Rule を設定し、直接pushを禁止してPR経由のマージのみを許可する
- 上記のCI状況を踏まえ、現時点ではステータスチェックの必須化は行わない
