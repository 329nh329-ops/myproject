# Trello風タスク管理アプリ 要件定義書

## 関連ドキュメント
本書は全体像をまとめた親ドキュメントであり、詳細は以下の各ドキュメントを参照する。

| ドキュメント | 内容 |
|---|---|
| [機能要件.md](./docs/機能要件.md) | カードの情報項目、新規作成・編集・削除・移動・永続化・API仕様の詳細 |
| [画面設計.md](./docs/画面設計.md) | 画面レイアウト、カード表示要素、フォーム構成、ドラッグ＆ドロップ時の見た目 |
| [データベース設計.md](./docs/データベース設計.md) | ER図、テーブル定義 |
| [技術スタック.md](./docs/技術スタック.md) | 使用言語・フレームワーク・DB製品・コード品質チェック体制 |

---

## 1. 概要

### 1.1 プロジェクト名
Trello風タスク管理アプリ（個人利用・学習課題用）

### 1.2 背景
本プロジェクトは、プログラミングスクールの課題として開発するものである。学習目的として、以下の技術・進め方を実践することを狙いとしている。

- HTML / CSS / JavaScript による基礎的なフロントエンド実装力（DOM操作、イベント処理、データ永続化）の習得
- ドラッグ＆ドロップAPIなど、実務でも使われるブラウザAPIの理解
- システム開発における「要件定義 → 設計 → 実装」という一般的な開発の流れの体験

実際の業務システムのように顧客が存在する開発ではないが、要件定義から着手する練習として、顧客への説明資料を想定した粒度でドキュメントを作成する。

### 1.3 目的
Trelloのような「リスト × カード」形式のタスク管理ツールを、開発者本人が1人で使うために作成する。

### 1.4 想定利用者
- 開発者本人のみ（個人利用）
- ユーザー登録・ログイン・複数ユーザー対応は不要

### 1.5 動作環境
- ブラウザで動作するフロントエンドと、データを永続化するバックエンド（サーバー）＋データベースで構成するアプリケーション
- フロントエンドはバックエンドのAPIを通じてデータの取得・登録・更新・削除を行う
- データはデータベースに保存され、ブラウザやPCを変えてもサーバーにアクセスすればデータが復元される
- マルチユーザー対応は行わない（利用者は開発者本人1人のみを前提とし、ユーザー管理・認証機能は持たない）

---

## 2. 用語定義

| 用語 | 説明 |
|---|---|
| リスト | タスクの進行状況を表す列。「未着手」「進行中」「完了」の3種類を固定で用意する |
| カード | 1件のタスクを表す単位。タイトル・説明文・優先度・期限の情報を持つ |
| インライン編集 | ページ遷移やポップアップを使わず、その場でカードの表示が入力フォームに切り替わる編集方式 |
| ドラッグ＆ドロップ | マウスでカードをつかんで移動し、別のリストまたは同一リスト内の別の位置で離すことでカードを移動・並び替える操作 |
| 表示順 | リスト内でのカードの並び順を表す値。ドラッグ＆ドロップや並び替えボタンの操作のたびに更新される |
| API | フロントエンドとバックエンドがデータをやり取りするための窓口。カードの取得・作成・更新・削除の操作はAPI経由で行う |
| データベース（DB） | カードやリストの情報を永続的に保存する仕組み。サーバーが管理し、複数の端末・ブラウザからアクセスしても同じデータが参照できる |

---

## 3. スコープ

### 3.1 対象とする機能（実装する）
- リスト（未着手・進行中・完了）の表示
- カードの新規作成
- カードの編集（全項目編集可能）
- カードの削除
- カード内容の永続化（データベースへの保存）
- カードのドラッグ＆ドロップによるリスト間移動・同一リスト内での並び替え
- 優先度順・期限順ボタンによるカードの並び替え
- フロントエンドとバックエンド間のAPI通信

機能の詳細は [機能要件.md](./docs/機能要件.md) を参照。

### 3.2 対象外（実装しない）
- ユーザー登録・ログイン・認証（マルチユーザー対応は行わない）
- 複数人での共有・リアルタイム同期
- リストの追加・削除（リストは固定の3種類）
- カードのラベル・色分け
- 通知・リマインダー機能

---

## 4. ユースケース・業務フロー

### 4.1 利用シーン
利用者（開発者本人）が日々の学習・開発タスクを管理する場面を想定する。

| No | シーン | 操作の流れ |
|---|---|---|
| UC-1 | 新しいタスクを思いついた | 「未着手」リストでカードを新規作成し、タイトル・説明・優先度・期限を入力する |
| UC-2 | タスクに着手する | 「未着手」リストのカードを「進行中」リストへドラッグ＆ドロップで移動する |
| UC-3 | タスクの内容を変更したい | カードをクリックしてインライン編集フォームを開き、内容を修正して保存する |
| UC-4 | タスクが完了した | 「進行中」リストのカードを「完了」リストへドラッグ＆ドロップで移動する |
| UC-5 | 不要になったタスクを消す | カードをクリックして編集フォームを開き、フォーム内の削除ボタンを押して確認ダイアログで削除を確定する |
| UC-6 | 翌日以降にアプリを開き直す | ページを再読み込みしても、データベースに保存された前回の状態がそのまま復元されている |
| UC-7 | 別のPC・ブラウザから開く | サーバーにアクセスすれば、同じデータが表示される（DB保存のため端末に依存しない） |
| UC-8 | リスト内のタスクを並び替えたい | カードをドラッグして同一リスト内の別の位置にドロップし、順番を入れ替える |
| UC-9 | 優先度・期限で整理したい | リストの「優先度順」または「期限順」ボタンを押し、そのリスト内のカードを一括で並び替える |

### 4.2 業務フロー図（イメージ）

```
[タスク発生] → (UC-1)カード作成@未着手
                    ↓ ドラッグ＆ドロップ(UC-2)
              @進行中で作業・随時編集(UC-3)
                    ↓ ドラッグ＆ドロップ(UC-4)
              @完了
                    ↓（不要なら）
              削除(UC-5)
```

---

## 5. 機能要件（概要）

カードは「タイトル（必須）」「説明文（任意）」「優先度（高・中・低）」「期限（任意）」の4項目を持ち、各リスト内でのインライン操作により新規作成・編集・削除ができる。カードは別のリストへドラッグ＆ドロップで移動でき、同一リスト内でも自由に並び替えられる。加えて「優先度順」「期限順」ボタンにより、そのリスト内のカードを一括で並び替えることもできる（一度きりの整列であり、以降の操作で順序は自由に崩せる）。すべての操作結果はAPIを通じてデータベースに保存される。

詳細な仕様（入力チェック、エラー表示、API一覧など）は [機能要件.md](./docs/機能要件.md) を参照。

---

## 6. 画面仕様（概要）

3つのリストを横に並べ、各リスト内にカードを縦に並べる1画面構成。カードのクリックでインライン編集フォームが展開され、リスト下部の「＋カードを追加」から新規作成フォームが展開される。

詳細なレイアウト・UI要素は [画面設計.md](./docs/画面設計.md) を参照。

---

## 7. 非機能要件

| 分類 | 項目 | 内容 |
|---|---|---|
| 動作環境 | 対応ブラウザ | 最新版の Google Chrome を正式サポート対象とする。他ブラウザでの動作は保証しない（学習目的のため対象を絞る） |
| 動作環境 | 画面サイズ | PC画面（横幅1024px以上）での利用を前提とする。スマートフォン・タブレット向けのレスポンシブ対応は行わない |
| 性能 | 想定データ量 | カード総数は最大50件程度を想定する。この範囲において、カードの追加・編集・削除・移動の操作結果は体感1秒未満で画面に反映されること |
| 操作性 | 誤操作防止 | カード削除など取り消しのできない操作の前には、必ず確認ダイアログを表示する |
| 操作性 | 入力チェック | 必須項目（タイトル）が未入力の状態では保存できないようにし、その理由を利用者に明示する |
| 可用性 | 稼働環境 | 個人のローカル環境またはローカルネットワーク内での稼働を前提とする |
| 保守性 | コード構成 | フロントエンド（HTML／CSS／JavaScript）とバックエンド（API・DBアクセス処理）を分離し、可読性・保守性を保つ |
| セキュリティ | データの扱い | 個人利用・学習目的のため、認証機能は持たないが、SQLインジェクション等の基本的な脆弱性対策は実装する |
| セキュリティ | 公開範囲 | 学習用ツールのため、インターネットに公開せず、開発者本人のローカル環境またはローカルネットワーク内での利用を前提とする |
| データ管理 | バックアップ | データベースに保存するが、明示的なバックアップ機能は持たない（下記8章のリスクを参照） |

---

## 8. データの扱い・リスクに関する説明

学習目的の個人利用ツールであることを踏まえ、以下の制約・リスクを許容した上で開発するものとする。

- データはサーバー側のデータベースに保存されるため、ブラウザのキャッシュ削除等では消失しない。ただし、データベースファイル自体の削除やサーバー環境の初期化を行った場合はすべて消失する。
- 認証機能を持たないため、サーバーにアクセスできる状態であれば誰でもデータの閲覧・変更ができてしまう。個人のローカル環境での利用に留め、インターネットへの公開は行わないことでこのリスクを回避する。
- マルチユーザー対応を行わないため、全データは単一の利用者（開発者本人）のものとして扱う。将来的に複数人での利用を想定する場合は、別途ユーザー管理・認証の仕組みの追加が必要となる。
- 上記の制約は、本アプリが学習用・個人用の簡易ツールであるという前提のもとで受け入れられるものである。

---

## 9. 受け入れ基準（完了の定義）

以下をすべて満たした時点で、本アプリの実装完了とみなす。

- [x] 「未着手」「進行中」「完了」の3リストが表示される
- [x] 各リストで「＋カードを追加」からカードを新規作成でき、タイトル・説明文・優先度・期限を保存できる
- [x] タイトル未入力の場合、エラーメッセージが表示され保存されない
- [x] カードをクリックすると編集フォームが開き、全項目を修正して保存できる
- [x] カードをクリックして開く編集フォーム内の削除ボタンから、確認ダイアログを経てカードを削除できる
- [x] カードをドラッグ＆ドロップで別のリストへ移動できる
- [x] カードをドラッグ＆ドロップで同一リスト内の任意の位置に並び替えられる
- [x] リストの「優先度順」ボタンでそのリスト内のカードが優先度順に並び替わる
- [x] リストの「期限順」ボタンでそのリスト内のカードが期限順に並び替わる
- [x] カードの作成・編集・削除・移動・並び替えの内容が、ページを再読み込みしても保持されている（データベースからの復元）
- [ ] 別のブラウザ・別のPCからサーバーにアクセスしても、同じデータが表示される（未検証）
- [ ] Google Chromeの最新版で、上記すべての操作が問題なく行える（他ブラウザでの動作確認は未実施）

---

## 10. 制約条件・前提条件

| 項目 | 内容 | 根拠 |
|---|---|---|
| 開発期間 | スクールのカリキュラム進行に合わせる | 課題としての提出期限があるため |
| 開発体制 | 開発者1名で設計から実装まで担当 | 個人学習課題のため |
| 使用技術 | フロントエンド：TypeScript + React（Vite）+ Tailwind CSS／バックエンド：Java + Spring Boot（Gradle、Spring Data JPA）／データベース：PostgreSQL。詳細は [技術スタック.md](./docs/技術スタック.md) を参照 | 実務でも広く使われる構成を学習するため |
| ユーザー対応 | マルチユーザー対応は行わない | 個人利用のみを前提とするため |
| 対応ブラウザ | Google Chrome最新版のみ | 開発・検証の効率を優先し、対象を絞るため |

---

## 11. 開発の進め方（マイルストーン）

| No | マイルストーン | 内容 | 確認方法 |
|---|---|---|---|
| 1 | 環境構築 | 決定済みの技術スタック（TypeScript + React／Spring Boot／PostgreSQL）で開発環境を整える | 環境構築後、疎通確認を行う |
| 2 | DB設計・構築 | [データベース設計.md](./docs/データベース設計.md) のER図をもとにテーブルを作成する | テーブル定義を目視確認 |
| 3 | API実装 | カードの取得・作成・更新・削除・移動のAPIを実装する | ツール（curl等）でAPIを直接呼び出して確認 |
| 4 | 画面レイアウト作成 | HTML/CSSで3リストとダミーカードの静的表示を作る | 見た目を目視確認 |
| 5 | フロント⇔API連携（作成・一覧取得） | カード新規作成とAPI経由での一覧表示を実装する | 実際にカードを追加し、DBに登録されるか確認 |
| 6 | カード削除機能 | 削除ボタン＋確認ダイアログ、削除APIとの連携を実装する | 削除操作を実施しDBから消えるか確認 |
| 7 | カード編集機能 | インラインフォームでの編集、更新APIとの連携を実装する | 編集して保存し、DBに反映されるか確認 |
| 8 | ドラッグ＆ドロップ・並び替え | リスト間移動、同一リスト内の並び替え、優先度順・期限順ボタン、それぞれのAPIとの連携を実装する | 実際に移動・並び替え・ソートボタンを操作し、DBに反映されるか確認 |
| 9 | 全体調整・受け入れ確認 | 見た目調整、9章の受け入れ基準を1つずつ確認する | チェックリストに沿って確認 |

---

## 12. 今後の検討事項（本要件には含めないが将来的な拡張候補）

- リストの追加・削除・並び替え
- カードのラベル・色分け
- 期限切れカードの強調表示の高度化（通知など）
- 完了カードの一括削除・アーカイブ
- データのエクスポート／インポート機能（バックアップ対策）
- マルチユーザー対応（ユーザー管理・認証機能の追加）
