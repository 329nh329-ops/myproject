# Trello風タスク管理アプリ 要件定義書

## 1. 概要

### 1.1 プロジェクト名
Trello風タスク管理アプリ（個人利用・学習課題用）

### 1.2 目的
- スクールの課題としてタスク管理アプリを作成する
- Trelloのような「リスト × カード」形式で、自分1人が使うタスク管理ツールを作る
- HTML / CSS / JavaScript の基礎的な実装力（DOM操作、イベント処理、データ永続化）を身につける

### 1.3 想定利用者
- 開発者本人のみ（個人利用）
- ログイン機能・複数ユーザー対応は不要

### 1.4 動作環境
- Webブラウザ上で完結するクライアントサイドのみのアプリケーション
- サーバー・データベースは使用しない
- データはブラウザの `localStorage` に保存し、再訪時も復元される

---

## 2. スコープ

### 2.1 対象とする機能（実装する）
- リスト（未着手・進行中・完了）の表示
- カードの新規作成
- カードの編集（全項目編集可能）
- カードの削除
- カード内容の永続化（localStorage）
- カードのドラッグ＆ドロップによるリスト間移動

### 2.2 対象外（実装しない）
- ユーザー登録・ログイン・認証
- 複数人での共有・リアルタイム同期
- サーバーサイド処理・データベース接続
- リストの追加・削除（リストは固定の3種類）
- カードのラベル・色分け
- 通知・リマインダー機能

---

## 3. 機能要件

### 3.1 リスト表示
- 画面に以下3つのリストを横に並べて表示する
  1. 未着手
  2. 進行中
  3. 完了
- 各リストには、そのリストに属するカードを縦に並べて表示する

### 3.2 カードの情報項目
各カードは以下の情報を持つ。

| 項目 | 必須/任意 | 型 | 備考 |
|---|---|---|---|
| タイトル | 必須 | 文字列 | 未入力の場合は保存不可 |
| 説明文 | 任意 | 文字列（複数行） | 詳細メモ用 |
| 優先度 | 必須 | 高 / 中 / 低 の選択式 | デフォルト値を用意する（例: 中） |
| 期限 | 任意 | 日付 | カレンダー入力（`<input type="date">`） |

### 3.3 カードの新規作成
- 各リストの下部（または上部）に「＋カードを追加」のような操作トリガーを設置する
- トリガーをクリックすると、そのリストの中にインラインで入力フォームが展開される
  - 入力項目：タイトル・説明文・優先度・期限
- 「追加」ボタンで保存し、フォームは閉じてカードとして表示される
- 「キャンセル」操作でフォームを閉じられる
- タイトル未入力の場合はエラー表示し、保存させない

### 3.4 カードの編集
- カードをクリックすると、そのカードの表示がインラインで編集フォームに切り替わる
- 全項目（タイトル・説明文・優先度・期限）を編集できる
- 「保存」で変更を反映し、「キャンセル」で変更を破棄して元の表示に戻す

### 3.5 カードの削除
- 各カードに削除ボタンを設置する
- クリック時、確認ダイアログ（`confirm()`など）を出してから削除する

### 3.6 カードの移動（ドラッグ＆ドロップ）
- カードをマウスでドラッグし、別のリストにドロップすることでリスト間を移動できる
- 同一リスト内での並び替えは必須要件としない（余裕があれば対応）
- ドロップ先が受付可能であることが視覚的にわかるようにする（ドロップ領域のハイライト等）
- HTML5 Drag and Drop API（`draggable`, `dragstart`, `dragover`, `drop` イベント）を用いて実装する

### 3.7 データの永続化
- カードの作成・編集・削除・移動が発生するたびに、全カード情報を `localStorage` に保存する
- ページ読み込み時に `localStorage` からデータを読み込み、画面に復元する
- 保存形式：カードの配列をJSON文字列化して保存（各カードがリストIDまたはステータスを持つ）

---

## 4. 非機能要件

| 項目 | 内容 |
|---|---|
| 対応ブラウザ | 最新版の Chrome を主とする（学習目的のため他ブラウザは対象外） |
| レスポンシブ対応 | 不要（PC画面での利用を想定） |
| パフォーマンス | 個人利用・少数カードのみ想定のため特別な最適化は不要 |
| セキュリティ | 個人のローカル環境での利用のため特別な対策は不要 |
| データバックアップ | 不要（消えても再入力できる範囲の利用） |

---

## 5. 画面構成（イメージ）

```
+--------------------------------------------------------------------+
|  Trello風タスク管理                                                  |
+--------------------------------------------------------------------+
|  [ 未着手 ]         |  [ 進行中 ]         |  [ 完了 ]              |
|  ------------------ |  ------------------ |  ------------------   |
|  [カード1]           |  [カード3]           |  [カード5]             |
|  [カード2]           |  [カード4]           |                        |
|                      |                      |                        |
|  ＋ カードを追加      |  ＋ カードを追加      |  ＋ カードを追加        |
+--------------------------------------------------------------------+
```

- カードクリック → インライン編集フォームに変化
- ＋クリック → インライン新規作成フォームが展開
- カードはドラッグして別リストへドロップ可能

---

## 6. 技術構成

| 項目 | 内容 |
|---|---|
| フロントエンド | HTML / CSS / JavaScript（素のJS、フレームワーク不使用） |
| データ保存 | ブラウザ `localStorage` |
| ファイル構成 | `index.html` / `style.css` / `script.js` の3ファイル構成 |

---

## 7. 開発の進め方（マイルストーン案）

1. 静的な画面レイアウト作成（HTML/CSS、3リストとダミーカードを表示）
2. カードの新規作成機能（インラインフォーム）
3. カードの削除機能
4. localStorageへの保存・読込機能
5. カードの編集機能（インラインフォーム）
6. ドラッグ＆ドロップによるリスト間移動
7. 全体の見た目調整・動作確認

---

## 8. 今後の検討事項（本要件には含めないが将来的な拡張候補）

- リストの追加・削除・並び替え
- カードのラベル・色分け
- 同一リスト内でのカード並び替え
- 期限切れカードの強調表示
- 完了カードの一括削除・アーカイブ
